.container-fluid

  =render partial: 'heading'

  -if(can? :manage, Dataset)

    -if @accounts && @accounts.count > 0
      %h3 Accounts
      %table.table
        %tr.row
          %th.col-md-3
            Name
          %th.col-md-3
            Email
          %th.col-md-3
            Expires
          %th.col-md-3(colspan="2" )
            Actions
      -@accounts.each do |account|
        %tr.row
          %td.col-md-3
            =account.name
          %td-col-md-3
            =account.email
          %td-col-md-3
            =Time.now.iso8601
          %td-col-md-1
            %button(type="button" class="btn btn-success btn-block" )
              Edit
          %td-col-md-1
            %button(type="button" class="btn btn-danger btn-block" )
              Destroy
    .pull-right
      %button(type="button" class="btn btn-primary" onclick='window.location = "/data_curation_network/account/add"' )
        %span(class="glyphicon glyphicon-plus" )
        Add





  -else
    .jumbotron
      This page is for administration of the Data Curation Network Portal for Illinois Data Bank. If you are an administrator, log in to manage accounts.
