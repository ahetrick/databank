<% flash.each do |key, value| %>
  <div class="alert alert-info"><%= sanitize(value) %></div>
<% end %>
<%= render partial: 'deposit_agreement_modal', :layout => false %>
<%= render partial: 'dashboard_new' %>

<%= render partial: 'form' %>


