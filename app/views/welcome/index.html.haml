.welcome-div
  = render partial: 'welcome'
  - if current_user && current_user.role == 'admin'
    .curator-only
      %a(href="/researcher_spotlights" class="btn btn-success idb") Manage Researcher Spotlight
    = render partial: 'role_switch_form'