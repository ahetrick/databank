.welcome-div
  = render partial: 'welcome'
  - if current_user && current_user.role == 'admin'
    .curator-only
      %a(href="/featured_researchers" class="btn btn-success idb") Manage Featured Researchers
    = render partial: 'role_switch_form'